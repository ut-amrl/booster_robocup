program: ./isaaclab.sh
name: PowerReward
method: grid
metric:
  name: Sweep/performance
  goal: maximize
command:
  - ${env}
  - ${program}
  - -p
  - scripts/rsl_rl/train.py
  - --task
  - T1-Baseline-v1
  - --headless
  - --video
  - --json_overrides
  - ${args_json_file}
parameters:
  env.rewards.power.weight:
    values: [-0.02, -0.0002]
    distribution: categorical