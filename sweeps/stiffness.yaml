program: ./isaaclab.sh
name: StiffnessDistribution
method: grid
metric:
  name: Sweep/performance
  goal: maximize
command:
  - ${env}
  - ${program}
  - -p
  - scripts/rsl_rl/train.py
  - --task
  - T1-Baseline-v1
  - --headless
  - --video
  - --json_overrides
  - ${args_json}
parameters:
  env.events.actuator_gains.params.stiffness_distribution_params.0:
    values: [1.00, 3.15, 10.00]
    distribution: categorical
  env.events.actuator_gains.params.stiffness_distribution_params.1:
    values: [1.10, 11.00, 100.00]
    distribution: categorical